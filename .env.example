# Copy this file to .env and fill in real values. Never commit .env — it's already in .gitignore.

# Supabase Project Settings > Database > Connection string (URI tab).
# Replace [YOUR-PASSWORD] with your actual database password.
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxx.supabase.co:5432/postgres
